# 报告目录说明

本目录用于保存“可阅读结果 + 原始数据”，规则如下：
- 文件名使用英文；内容使用中文。
- 每个框架至少包含 1 份中文说明（`details/*_report.md`）。
- 每个框架都包含数据测试结果（`data/*.json` 或 `logs/*benchmark.log`）。
- 所有模块单元测试合并到 `logs/unit_test_all.log`，方便一次排查。

推荐先读：`summary_report.md`
